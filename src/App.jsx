import React from 'react';

/**
 * App component is currently not used as the main entry point.
 * Routing is handled directly in main.jsx via RouterProvider.
 */
const App = () => {
  return null;
};

export default App;
